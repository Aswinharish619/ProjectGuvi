package task11package;

public class CheckedandUnchecked {

	// Checked Exception:

	/*
	 * Method causeCheckedException throws a checked exception (Exception).
	 * The main method catches this exception using a try-catch block, demonstrating how
	 * checked exceptions must be either caught or declared in the method signature.
	 */

	// Unchecked Exception:

	/*
	 * Method causeUncheckedException throws an unchecked exception
	 * (NullPointerException), a subclass of RuntimeException. 
	 * The main method catches this exception as well, even though catching unchecked exceptions is not
	 * mandatory.
	 */

}
